{
  "_comment": "Per-deployment config. For a same-box Onfinity, point Vaapi:BaseUrl at the loopback VAAPI instead. No credentials here — callers send accessKey/secretKey headers (pass-through).",
  "Vaapi": {
    "BaseUrl": "https://erplive.viennaadvantage.com/api/VAAPI/Service"
  }
}
